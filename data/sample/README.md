# data/sample/

Ten tiny placeholder images (one per class) used to smoke-test `notebooks/04_demo.ipynb` and `python -m src.inference`.

These are synthetic — for the demo video, replace them with real images saved from the CIFAR-10 test split or your own photos.
